# Railway's build environment sets NODE_ENV=production before npm ci runs,
# which makes npm silently skip devDependencies (tsx, vite, esbuild, etc.) —
# breaking `npm run build`. Force them to install regardless.
production=false
